---

skip_list:
  # - no-changed-when           # '301'  # Commands should not change things if nothing needs doing
  # - command-instead-of-module # '303'  # Using command rather than module
  # - risky-shell-pipe          # '306'  # Shells that use pipes should set the pipefail option
  - fqcn-builtins
...
